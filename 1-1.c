/*Realizar un programa en C en el que se declare una variable entera, se asigne un valor
que Ud. elija y se la presente por pantalla.*/
#include int main()
{
int variable;
printf(" E culiao ingresa un numero");
scanf("%i", &variable);
printf(" El numero que ingreso este culiao es el %i", variable);
return 0;
}
